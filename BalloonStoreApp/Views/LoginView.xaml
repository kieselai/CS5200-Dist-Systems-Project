<UserControl x:Class="BalloonStoreApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="60">
        <CheckBox x:Name="isLocal" Content="Use local registry" IsChecked="{Binding IsLocal}" HorizontalAlignment="Stretch" Margin="0" Foreground="White" VerticalAlignment="Top" Width="122" Grid.ColumnSpan="2" Height="18"/>
        <Button x:Name="startGame" Content="Login" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Width="125" Command="{Binding InitiateLogin}"  Margin="0" Grid.ColumnSpan="2" Height="22"/>
    </Grid>
</UserControl>
