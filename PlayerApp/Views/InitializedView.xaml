<UserControl x:Class="PlayerApp.Views.InitializedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="auto" Height="auto">
        <ContentControl x:Name="CommonInfo" prism:RegionManager.RegionName="CommonInfoRegion" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0"/>
        <ContentControl x:Name="OwnInfo"    prism:RegionManager.RegionName="OwnInfoRegion"    HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0"/>
    </Grid>
</UserControl>
