<UserControl x:Class="BalloonStoreApp.Views.LogoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="120" Height="20">
        <Button x:Name="logout" Content="Log out" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Width="125" Command="{Binding InitiateLogout}"  Margin="0" Grid.ColumnSpan="2" Height="22"/>
    </Grid>
</UserControl>
